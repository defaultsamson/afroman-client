MUSIC:true
SERVER_USERNAME:if (getCurr
SERVER_PASSWORD:
SERVER_IP:127.0.0.1
SERVER_PORT:6969
CLIENT_USERNAME:Bunn
CLIENT_PASSWORD:
CLIENT_IP:if (getCurrentScreen() != null)
			{
				getCurrentScreen().tick
CLIENT_PORT:
FULLSCREEN:false
